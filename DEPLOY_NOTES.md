Mobile Quick Checklist (if GPS not captured):
1. Open the GitHub Pages URL in Chrome (not inside WhatsApp/Gmail preview).
2. When browser asks for location permission, tap Allow.
3. If you denied earlier: go to Chrome Settings → Site Settings → Location → find the site and allow.
4. If testing locally via a PC, opening the file using file:/// will NOT prompt for location on mobile.
